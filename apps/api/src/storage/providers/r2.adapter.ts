/**
 * R2Adapter, Cloudflare R2 endpoint'i ile yapılandırılmış S3StorageProvider'dır.
 * AWS S3 SDK, S3 uyumlu herhangi bir sağlayıcıya (R2, MinIO, S3) karşı çalışır.
 */
export { S3StorageProvider as R2Adapter } from '../s3-storage.provider';
